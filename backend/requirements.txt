# Core web framework
Flask==3.1.0
flask-cors==4.0.0

# HTTP requests
requests==2.32.3

# Image processing
Pillow==11.0.0
exifread==3.0.0

# PostgreSQL driver (stable)
psycopg[binary]==3.2.10

# WSGI server for production
gunicorn==23.0.0

# Optional ORM (if used in the future)
SQLAlchemy==2.0.22

# Optional: Load environment variables from .env
python-dotenv==1.1.0

# Werkzeug compatible with Flask 3.1
Werkzeug>=3.1.0
